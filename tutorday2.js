// Mảng, khai báo, truy xuất mảng
// Làm việc với mảng
// Vòng Lặp For OF
// Hàm, khai báo hàm, các loại hàm, biến toàn cục, và biến cục bộ
// Đối tượng, làm việc với đối tượng
// Vòng lặp FOR IN
// Sự kiện, Events

// mảng trong JS có thể chứa bất kỳ kiểu dữ liệu

//let myArr = []; -> khai báo mảng rỗng

// let myClass = ['SD1802',32,{name : `JS`}];
// console.log(myClass);
// myClass[0] = `SD1801`;
// console.log(myClass);
// Truy cập và thay đổi phần tử trong mảng
// Các phương thức thường dùng
// push() - để thêm 1, hoặc nhiều phần tử vào cuối mảng
 //let PtcdClass = [`SD1801`,'SD1802','SD1803'];
 //tên mảng.push(giá trị muốn thêm)
 //PtcdClass.push('SD1804');
 //console.log(PtcdClass);
 //PtcdClass.pop() - xoá  phần tử cuối cùng
 //shift() - xoá phần tử đầu tiên
//splice() thêm, xoá hoặc sửa phần tử cuả mảng tại vị trí cụ thể
//array.splice(index,số lượng phần tử muốn xoá, phần tử muốn thêm)
// PtcdClass.splice(1,1,`SD1804`);
// console.log(PtcdClass);
// For Of - lặp qua all phần tử trong mảng
// arr [ {name : `Sd1801`, studentnum = 30},]
// Hàm Built in Function 
// Hàm tự xây dựng 
// Biến cục bộ và Biến toàn cục
// let a = 5; // biến toàn cục global
// function showNumber(){
//     let b = 6;// biến cục bộ block
// }
// console.log(b);
//Objects là 1 kiểu dữ liệu key : value
// let student ={
//     name : `Hihi`,
//     age : 17,
//     className : `SD1808`
//     // key : value
// }
// for (let key in student) {
//         console.log(`${key} : ${student[key]}`);
       
//}
// Events, on + V ( động từ)
// vị trí để viết sự kiện
//  onclick on+V : viết ở open tag, trong html
// Dùng DOM sau đó gọi sự kiện onclick ở bên js 
// addeventlistener : hỗ trợ xử lý đa tác vụ







